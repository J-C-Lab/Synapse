{
  "name": "create-synapse-plugin",
  "version": "0.4.0",
  "description": "Scaffold a new Synapse plugin project",
  "license": "MIT",
  "bin": {
    "create-synapse-plugin": "./dist/index.js"
  },
  "files": [
    "README.md",
    "dist",
    "template"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.8.2",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@synapsepkg/plugin-cli": "workspace:*",
    "@synapsepkg/plugin-manifest": "workspace:*"
  },
  "publishConfig": {
    "access": "public"
  }
}
