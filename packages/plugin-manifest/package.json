{
  "name": "@synapsepkg/plugin-manifest",
  "version": "0.3.0",
  "description": "Synapse plugin manifest schema, validation, and engine-compatibility helpers — shared by the Synapse host and the plugin CLI",
  "license": "MIT",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "README.md",
    "dist",
    "schema",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/gen-schema.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@synapsepkg/plugin-sdk": "workspace:*",
    "zod": "^4.4.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
